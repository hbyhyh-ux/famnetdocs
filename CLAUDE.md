# CLAUDE.md

팜넷 문서발행 시스템. 견적서·거래명세서·발주서·원산지증명서를 발행하고
구글 시트 원장에 기록하는 사내 도구.

## 구조

```
src/index.html      화면 전체 (HTML+CSS+JS 단일 파일, 편집은 여기서)
src/fonts/          서브셋한 A고딕 woff2 (400/700)
src/img/            팜넷 로고 png
apps-script/Code.gs 서버 — 채번, 원장 읽기/쓰기, 담당자 명부
build/build.js      폰트·로고를 base64로 인라인 → dist/index.html
dist/index.html     배포본 (빌드 산출물, 직접 편집하지 말 것)
```

빌드: `node build/build.js`

## 배포 방법

이 앱은 **구글 시트에 붙은 Apps Script 웹앱**으로 돌아간다. 빌드나 CI가 없고,
편집기에 붙여넣는 방식이다.

1. 원장 구글 시트 → 확장 프로그램 → Apps Script
2. `Code.gs` 를 바꿨으면 `apps-script/Code.gs` 내용으로 교체
3. 화면을 바꿨으면 `node build/build.js` 후 `dist/index.html` 내용을
   Apps Script 의 `index.html` 파일에 교체
4. **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**
   — 저장만 하면 반영되지 않는다. 반드시 새 버전으로 재배포해야 한다.

웹앱 URL 은 재배포해도 바뀌지 않으므로 사용자에게 새 링크를 줄 필요가 없다.

## 이 환경에서 반드시 지켜야 하는 제약

**외부 자원을 쓸 수 없다.** Apps Script 웹앱은 정적 파일을 서빙하지 못한다.
폰트·이미지·스크립트 전부 단일 HTML 안에 인라인되어야 한다. CDN 링크를 추가하면
안 된다. 이것이 빌드 단계가 존재하는 이유다.

**CSS 순서가 곧 우선순위다.** 단일 `<style>` 안에서 특이도가 같은 규칙은
뒤에 온 것이 이긴다. 스타일시트는 이 순서를 지켜야 한다:

```
:root 토큰 → 기본 컴포넌트 → 문서(.paper) → 반응형 → @media print
```

과거에 반응형 블록이 기본 컴포넌트보다 앞에 있어서 모바일 규칙이 전부
무시된 버그가 있었다. 반응형 규칙을 추가할 때는 반드시 `@media print`
직전에 넣을 것.

**`fetch` 로 서버를 부를 때 `Content-Type` 은 `text/plain`.**
JSON 으로 보내면 브라우저가 preflight(OPTIONS)를 보내는데 Apps Script 는
이를 처리하지 못해 요청이 서버에 닿지도 못하고 실패한다.
단, 웹앱으로 서빙될 때는 `google.script.run` 경로를 쓰므로 이 문제가 없다
(`HOSTED` 플래그로 분기).

**문서번호 채번은 서버에서만.** 전원이 동시에 발행하므로 `LockService` 로
잠근 구간 안에서 시트를 읽고 다음 번호를 발급한다. 클라이언트에서 계산하면
같은 번호가 중복 발급된다. (연결이 끊긴 경우에만 로컬 채번으로 폴백)

## 두 가지 실행 경로

| | 웹앱 (정상) | 파일 직접 열기 (폴백) |
|---|---|---|
| 서버 통신 | `google.script.run` | `fetch` + 팀 코드 |
| 연결 설정 | 불필요 (자동) | URL·팀 코드 입력 |
| 판별 | `HOSTED === true` | `HOSTED === false` |

`HOSTED` 는 `typeof google !== 'undefined' && google.script.run` 으로 판정한다.
기능을 추가할 때 양쪽 경로를 모두 지원해야 한다 (`api()` 함수에 액션 추가).

## 데이터 수명

설정마다 수명이 다르다. 이걸 섞으면 사용자가 혼란스러워한다.

| 항목 | 수명 | 저장 위치 |
|---|---|---|
| 열 이름·너비·형식 | **그 문서만** — 새 접속·새 문서·문서종류 변경 시 초기화 | 발행 시 문서에 함께 저장 |
| 담당자 명부 | 영구·팀 공용 | 시트 `담당자` |
| 도장 이미지 | 영구·개인 | 브라우저 |
| 웹앱 URL·팀 코드 | 영구·개인 | 브라우저 |
| 발행 기록·메모 | 영구·팀 공용 | 시트 `발행기록` |

과거 문서를 불러오면 **발행 당시의** 열 이름·너비·형식이 복원된다.
석 달 전 견적서를 재출력했을 때 모양이 달라지면 안 되기 때문이다.

## 시트 구조

`발행기록` 시트: 기록시각, 문서번호, 구분, 발행일자, 수신자, 발행자,
품목요약, 총계, VAT, **상세(JSON, J열 — 숨김, 지우면 안 됨)**, 메모(K열)

문서 전체 내용이 J열 JSON 에 들어 있고, 나머지 열은 사람이 읽기 위한 것이다.
`listDocs()` 가 J열을 파싱해 화면에 돌려준다.

`담당자` 시트: 담당자, 전화번호, 이메일, 수정시각

## 문서 종류

| 종류 | 접두사 | 열 구성 | 합계 |
|---|---|---|---|
| 견적서 (단가형) | `Q-` | 품명·규격·단가·배송비·비고 | 없음 |
| 견적서 (총액형) | `Q-` | 품명·규격·단가·수량·금액·비고 | 있음 + 기간란 |
| 거래명세서 | `T-` | 품명·규격·단가·수량·금액·비고 | 있음 |
| 발주서 | `B-` | 품명·규격·수량·비고 | 없음 + 납기 정보 |
| 원산지증명서 | `C-` | (표 없음, 나열형) | 없음 |

`layoutKey()` 가 종류+견적유형을 하나의 키로 합쳐 열 설정을 구분한다
(`quote` / `quote_total` / `invoice` / `order`).

## 부가세 처리

`incl` 포함(역산) / `excl` 별도(10% 가산) / `exempt` 면세 / `zero` 영세율.
표기는 표 밖 오른쪽 위(`.vat-note`)에 둔다 — 단가 열 머리글에 넣으면
열이 불필요하게 넓어진다.

## 폰트

원본 A고딕 ttf 는 각 3.6MB, 글리프 18,194개. 그대로 쓸 수 없어서
**상용 한글 2,346자 + 영숫자·기호**로 서브셋해 woff2 로 변환했다 (합 235KB).

트레이드오프: 아주 희귀한 글자가 든 거래처명은 그 글자만 대체 폰트로 보인다.
팜넷에서 쓰는 단어들로 검증해 누락은 없었지만, 문제가 생기면 완성형
11,172자 전체로 다시 서브셋해야 한다 (그 경우 1.1MB).

재서브셋 명령은 `docs/font-subset.md` 참고.

## 인쇄

`@media print` 에서 `.paper` 만 남기고 전부 숨긴다. 입력 패널을 추가하면
`.layout .panel` 로 이미 잡히지만, `.panel` 이 아닌 요소를 추가했다면
인쇄 숨김 목록에 직접 넣어야 한다.

미리보기는 화면에서 `transform: scale()` 로 축소되는데, 인쇄 시에는
`transform:none` 으로 풀어 실측 A4 로 출력한다.

## 접근 권한 관련

배포 시 "액세스 권한"을 `모든 사용자`로 두면 링크만 알면 누구나 들어와
발행 기록까지 볼 수 있다. `famnet.co.kr` 조직 전용으로 잠그는 것이 안전하지만,
그 경우 개인 구글 계정으로는 접속할 수 없다 (조직 정책이 외부 계정 접근을
차단하므로, 로그아웃 상태보다 개인 계정이 오히려 더 강하게 막힌다).

"다음 사용자 인증 정보로 실행"은 **반드시 `나`로 유지**해야 한다.
`웹 앱에 액세스하는 사용자`로 바꾸면 각자 권한으로 시트에 접근하게 되어,
시트가 공유되지 않은 사람은 전부 실패한다.

## 작업 시 주의

- `dist/index.html` 을 직접 고치지 말 것 (빌드 때 덮어써진다)
- base64 자원이 `src/index.html` 에 들어가지 않도록 할 것
  (`__FONT:이름__`, `__IMG:이름__` 자리표시자 유지. 빌드가 치환한다)
- 열 설정을 다루는 함수는 `layoutKey()` 를 거쳐야 한다 (`state.kind` 직접 사용 금지)
- 새 기능이 서버를 쓰면 `api()` 와 `Code.gs` 의 `doPost` 스위치, `ui*` 함수
  세 곳을 모두 손봐야 한다
